environments:
  ci:
  local:
repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami
releases:
  - name: mongodb
    chart: bitnami/mongodb
    version: 10.28.0
    values:
      - ./config/mongodb.yaml.gotmpl
